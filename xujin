#!/bin/bash
# Convenience wrapper to run the offline workflow executor.
# Usage: /xujin [args]

set -e

SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"

if [ -d "$SCRIPT_DIR/.venv" ]; then
    source "$SCRIPT_DIR/.venv/bin/activate"
fi

exec python -m xujin_workflow.executor "$@"
